<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ilya.ourfuture">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginPage.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistrationPage.RegisterActivity" />
        <activity android:name=".UserPage.UserActivity" />
        <activity android:name=".PeopleList.PeopleListActivity" />
        <activity android:name=".QuestionPage.QuestionActivity" />
        <activity android:name=".QuestionPage.QuestionPeopleListActivity" />
        <activity android:name=".Markers.MarkersActivity" />
        <activity android:name=".CreateQuestion.CreateQuestionConditionsActivity" />
        <activity android:name=".Tops.TopActivity" />
        <activity android:name=".Selection.SelectionActivity" />
        <activity android:name=".Followers.FollowersActivity" />
        <activity android:name=".CreateQuestion.CreateQuestionSettingsActivity" />
        <activity android:name=".VotersActivity"></activity>
    </application>

</manifest>